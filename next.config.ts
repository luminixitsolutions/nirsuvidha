import type { NextConfig } from 'next'

// Default Next.js on Vercel: no basePath, output, or distDir (do not set output: "export" unless intended).
const nextConfig: NextConfig = {}

export default nextConfig
