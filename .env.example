# Laravel API (Next.js calls /api/public/* and admin JSON under /api).
# Set this to your deployed admin/API base URL (no trailing slash issues — app normalizes paths).
NEXT_PUBLIC_API_URL=https://luminixprojects.in/nrisuvidha/public/admin
